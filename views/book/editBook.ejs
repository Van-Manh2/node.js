<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa sách</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .form-container {
            max-width: 700px;
            margin: 50px auto;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }

        .btn-primary {
            background-color: #0d6efd;
            border-color: #0d6efd;
        }

        .btn-primary:hover {
            background-color: #0b5ed7;
            border-color: #0b5ed7;
        }

        .form-label {
            font-weight: bold;
        }

        .form-container img {
            max-height: 150px;
            margin-bottom: 15px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="form-container">
            <h2 class="mb-4 text-center">Sửa sách</h2>
            <form action="/books/edit/<%= book._id %>" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="title" class="form-label">Tên sách</label>
                    <input type="text" id="title" name="title" class="form-control" value="<%= book.title %>" required>
                </div>
                <div class="mb-3">
                    <label for="author" class="form-label">Tác giả</label>
                    <input type="text" id="author" name="author" class="form-control" value="<%= book.author %>"
                        required>
                </div>
                <div class="mb-3">
                    <label for="genre" class="form-label">Thể loại</label>
                    <select id="genre" name="genre" class="form-select" required>
                        <option value="fiction" <%=book.genre==='fiction' ? 'selected' : '' %>>Tiểu thuyết</option>
                        <option value="non-fiction" <%=book.genre==='non-fiction' ? 'selected' : '' %>>Phi hư cấu
                        </option>
                        <option value="science" <%=book.genre==='science' ? 'selected' : '' %>>Khoa học</option>
                        <option value="history" <%=book.genre==='history' ? 'selected' : '' %>>Lịch sử</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="publishedYear" class="form-label">Năm xuất bản</label>
                    <input type="number" id="publishedYear" name="publishedYear" class="form-control"
                        value="<%= book.year %>" required>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Mô tả sách</label>
                    <textarea id="description" name="description" class="form-control" rows="4"
                        required><%= book.description %></textarea>
                </div>
                <div class="mb-3">
                    <label for="image" class="form-label">Ảnh bìa (nếu muốn thay đổi)</label>
                    <% if (book.image) { %>
                        <img src="<%= book.image %>" alt="Ảnh bìa hiện tại" class="img-thumbnail">
                        <% } %>
                            <input type="file" id="image" name="image" class="form-control" accept="image/*">
                </div>
                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-primary">Cập nhật sách</button>
                    <a href="/books/<%= book._id %>" class="btn btn-secondary">Hủy</a>
                </div>
            </form>
        </div>
    </div>
</body>

</html>