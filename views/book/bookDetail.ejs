<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết sách</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }

        .container {
            max-width: 800px;
            margin-top: 50px;
        }

        .book-cover {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="card shadow-sm">
            <div class="card-body">
                <h5 class="card-title text-center">
                    <%= book.title %>
                </h5>

                <!-- Kiểm tra nếu có ảnh, nếu không thì hiển thị ảnh mặc định -->
                <img src="<%= book.image || '/images/default-book.png' %>" alt="Ảnh bìa"
                    class="book-cover img-thumbnail mb-3" width="200px" height="200px">

                <p><strong>Tác giả:</strong>
                    <%= book.author %>
                </p>
                <p><strong>Thể loại:</strong>
                    <%= book.genre %> <!-- Sửa từ category thành genre -->
                </p>
                <p><strong>Năm xuất bản:</strong>
                    <%= book.year %>
                </p>
                <p><strong>Số bản có sẵn:</strong>
                    <%= book.available %> <!-- Sửa từ availableCopies thành available -->
                </p>
                <p><strong>Mô tả:</strong>
                    <%= book.description || 'Chưa có mô tả' %> <!-- Thêm điều kiện nếu không có mô tả -->
                </p>
                <div class="text-center">
                    <a href="/books" class="btn btn-primary">Quay lại danh sách sách</a>
                    <a href="/books/edit/<%= book._id %>" class="btn btn-warning">Sửa sách</a>
                    <form action="/books/delete/<%= book._id %>" method="POST" style="display:inline;">
                        <button type="submit" class="btn btn-danger"
                            onclick="return confirm('Bạn có chắc chắn muốn xóa sách này không?');">Xóa sách</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>